node_modules/
dist/
src/generated/
website/.next/

# Playwright default output directory (local/CI)
test-results/

# TypeScript incremental cache (see tsconfig "incremental": true)
*.tsbuildinfo

website/public/openapi-commercial-v1.yaml
website/public/llms.txt
.next/

# Lighthouse CI (local/CI run output; config stays in repo e.g. lighthouserc)
.lighthouseci/

*.db
.DS_Store

# Operator-local telemetry pack exports (see docs/telemetry-daily-pack.md)
artifacts/telemetry-packs/

# scripts/validate-commercial-funnel.mjs single-instance lock (not committed)
artifacts/validate-commercial.lock

# Local env (secrets); keep .env.example committed
website/.env
website/.env.local
.env.local

# Cursor / Claude Code / agent workspace (open-source tree stays product-facing)
.cursor/
.claude/
.agents/

examples/.canonical-verify-run/
