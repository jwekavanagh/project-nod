---
surfaceKind: example
title: LangGraph checkpoint trust — verified Outcome Certificate (B-row) — AgentSkeptic
description: Static example of a LangGraph checkpoint trust Outcome Certificate with verified checkpoint rollup after read-only SQL matched structured tool parameters.
intent: Teams evaluating LangGraph checkpoint trust who want to see an approval-grade stdout artifact before wiring emitters and verify commands.
valueProposition: You see the same certificate shape and checkpoint verdicts the CLI prints on a successful B-row path, without leaving the marketing site.
primaryCta: integrate
route: /examples/langgraph-checkpoint-trust
evaluatorLens: false
embedKey: langgraph_checkpoint_trust
---

# LangGraph checkpoint trust (verified certificate)

This page shows a **real** Outcome Certificate from **`--langgraph-checkpoint-trust`** verify against the partner quickstart contract (`wf_partner`, `crm.upsert_contact`, v3 `tool_observed` with `langgraphCheckpoint`).

For copy-paste emitter and CLI commands, use **[`examples/langgraph-reference/README.md`](https://github.com/jwekavanagh/agentskeptic/blob/main/examples/langgraph-reference/README.md)** and the generated partner quickstart doc. **Normative terminal behavior** (A1–D, ineligible rules, production gate) lives only in **[`docs/langgraph-checkpoint-trust-ssot.md`](https://github.com/jwekavanagh/agentskeptic/blob/main/docs/langgraph-checkpoint-trust-ssot.md)**.

## What to do next

- Run first-read verification on [`/integrate`](/integrate).
- Return to the LangGraph guide at [`/guides/verify-langgraph-workflows`](/guides/verify-langgraph-workflows).
- Compare the trace-vs-database failure envelope on [`/examples/wf-missing`](/examples/wf-missing).
