{
  "schemaVersion": 1,
  "slug": "/database-truth-vs-traces",
  "readmeTitle": "AgentSkeptic — when traces say success but your database does not match",
  "heroTitle": "Your traces say \"success.\" Your database disagrees.",
  "heroSubtitle": "Verify your database state with read-only SQL before you ship, bill, or close. Not arbitrary log search—structured tool events (for example NDJSON), then read-only SQL against the actual database.",
  "homepageAcquisitionCtaLabel": "Product brief",
  "homepageDecisionFraming": "AgentSkeptic runs read-only SQL at verification time on Postgres or SQLite, comparing persisted rows to structured tool activity—before you ship, bill, or close.",
  "homepageHero": {
    "what": "AgentSkeptic turns tool parameters and registry rules into read-only SELECTs you can treat as a gate—separate from whether a step narrative read as successful.",
    "why": "Most reviews still treat traces and chat as proof of what landed in the database; that habit is how wrong rows slip through green CI.",
    "when": "Use it after a replay or deploy when you need a decisive check before customer-facing actions, finance touches, or a merge—not when you only want another log volume."
  },
  "visitorProblemAnswer": "Teams ship agent and automation workflows where traces, tool responses, and success flags look green while the database row is missing, stale, or wrong. AgentSkeptic compares structured tool activity to read-only SQL against your SQLite or Postgres at verification time and reports whether observed state matched expectations derived from what the workflow claimed—not whether the step narrative read as successful.\n\nUse it when you need persisted rows checked against declared tool parameters before customer-facing actions, compliance evidence, or CI gates.",
  "pageMetadata": {
    "title": "Database truth vs traces — AgentSkeptic",
    "description": "Traces say success while rows are missing or wrong. AgentSkeptic runs read-only SQL at verification time on Postgres or SQLite—ground truth from structured tool activity, not trace color alone."
  },
  "sections": [
    {
      "heading": "The moment it bites",
      "paragraphs": [
        "Your run finished with a green trace or a tool response that looks like success, but the ticket is still open in Postgres, the CRM row never landed, or CI passed while production would fail. Read-only SQL against authoritative tables is the forcing truth—not another narrative."
      ]
    },
    {
      "heading": "What AgentSkeptic adds",
      "paragraphs": [
        "You emit structured tool activity (for example NDJSON lines with tool IDs and parameters). The engine derives expected rows or fields under your registry rules and runs read-only `SELECT`s. You get verified, missing, or inconsistent with reason codes such as ROW_ABSENT—still at verification time, not as proof that a specific API call wrote the row."
      ]
    },
    {
      "heading": "Shareable failure pattern",
      "paragraphs": [
        "The bundled demo contrasts `wf_complete` (row matches) with `wf_missing` (success-shaped run, row absent)—the product in one glance."
      ]
    },
    {
      "heading": "What to do next",
      "paragraphs": [
        "Run the homepage demo, follow /integrate for first-run on your database, or install the CLI from the repository README. When you need licensed npm features, use the commercial package and API keys described on the site.",
        "To turn a verification run into a private, pasteable link, use the CLI with a share-report origin pointing at this site (HTTPS origin only; details ship with the open-source CLI). For deeper walkthroughs and bundled proof pages, see Learn (/guides), including bundled examples at /guides#bundled-proof."
      ]
    }
  ],
  "problemIndex": [
    {
      "moment": "Green LangGraph or agent trace but wrong or missing Postgres row",
      "primaryRoute": "/guides/trace-green-postgres-row-missing",
      "relatedRoutes": ["/integrate", "/database-truth-vs-traces"]
    },
    {
      "moment": "OpenAI-style tool loop reported success; CRM or SQLite state does not match",
      "primaryRoute": "/guides/tool-loop-success-crm-state-wrong",
      "relatedRoutes": ["/integrate", "/pricing"]
    },
    {
      "moment": "CI passed on workflow logs; database side effect never showed up",
      "primaryRoute": "/guides/ci-green-logs-row-absent",
      "relatedRoutes": ["/guides/ci-green-missing-database-side-effect", "/integrate"]
    },
    {
      "moment": "Need a gate before production: read-only verification, not more log volume",
      "primaryRoute": "/guides/pre-production-read-only-sql-gate",
      "relatedRoutes": ["/integrate", "/pricing"]
    },
    {
      "moment": "Green dashboards still happen when an AI agent writes wrong CRM data and you need read-only SQL before you trust the row.",
      "primaryRoute": "/guides/ai-agent-wrong-crm-data",
      "relatedRoutes": ["/database-truth-vs-traces"]
    },
    {
      "moment": "When automation reports success while database records disagree with declared tool parameters until read-only verification, treat trace color as non-authoritative.",
      "primaryRoute": "/guides/automation-success-database-mismatch",
      "relatedRoutes": ["/integrate"]
    },
    {
      "moment": "Debugging Postgres after a LangGraph run requires row-level truth at verification time beyond trace completion flags alone for customer-facing data.",
      "primaryRoute": "/guides/debug-postgres-after-langgraph",
      "relatedRoutes": ["/guides/verify-langgraph-workflows", "/integrate"]
    },
    {
      "moment": "When a Stripe webhook returns OK you still need the ledger database row to match before finance treats settlement as final and read-only SQL verifies persisted state.",
      "primaryRoute": "/guides/stripe-webhook-database-alignment",
      "relatedRoutes": ["/pricing"]
    },
    {
      "moment": "CI can stay green on workflow logs while the database side effect is missing until a read-only gate surfaces ROW_ABSENT before production.",
      "primaryRoute": "/guides/scenario-ci-green-side-effect-missing",
      "relatedRoutes": ["/guides/ci-green-missing-database-side-effect", "/integrate"]
    }
  ],
  "cliFollowupLines": [
    "More context (green trace vs database): {{ACQUISITION_URL}}",
    "First-run on your database: {{INTEGRATE_URL}}",
    "PatternComplete checklist (SSOT): https://github.com/jwekavanagh/agentskeptic/blob/main/docs/first-run-integration.md#adoptioncomplete_patterncomplete-normative",
    "Canonical site: {{ORIGIN}}/"
  ],
  "shareableTerminalDemo": {
    "title": "Pasteable terminal proof (bundled demo)",
    "transcript": "### Success (`wf_complete`)\n\nworkflow_id: wf_complete\nworkflow_status: complete\ntrust: TRUSTED: Every step matched the database under the configured verification rules.\nsteps:\n  - seq=0 tool=crm.upsert_contact result=Matched the database.\n\n{\n  \"schemaVersion\": 15,\n  \"workflowId\": \"wf_complete\",\n  \"status\": \"complete\",\n  \"steps\": [{ \"seq\": 0, \"toolId\": \"crm.upsert_contact\", \"status\": \"verified\" }]\n}\n\n### Failure (`wf_missing`)\n\nworkflow_id: wf_missing\nworkflow_status: inconsistent\nsteps:\n  - seq=0 tool=crm.upsert_contact result=Expected row is missing from the database (the log implies a write that is not present).\n    reference_code: ROW_ABSENT\n\n{\n  \"schemaVersion\": 15,\n  \"workflowId\": \"wf_missing\",\n  \"status\": \"inconsistent\",\n  \"steps\": [\n    {\n      \"seq\": 0,\n      \"toolId\": \"crm.upsert_contact\",\n      \"status\": \"missing\",\n      \"reasons\": [{ \"code\": \"ROW_ABSENT\" }]\n    }\n  ]\n}"
  },
  "llms": {
    "intentPhrases": [
      "green trace wrong database row agent workflow",
      "trace says success but CRM row missing",
      "read-only SQL check after LangGraph or automation run",
      "structured tool activity NDJSON verification",
      "ROW_ABSENT inconsistent workflow result"
    ],
    "notFor": [
      "unstructured log search as verification",
      "proof that a specific HTTP request caused a write",
      "non-SQL systems without a queryable mirror"
    ],
    "relatedQueries": [
      "green LangGraph trace postgres row missing",
      "how to verify AI agent wrote to postgres",
      "CI passed but database inconsistent side effect",
      "compare tool parameters to sql row verification",
      "agent run record workflow result json"
    ]
  },
  "readmeFold": {
    "templateLines": [
      "## {{HERO_TITLE}}",
      "",
      "{{HERO_SUBTITLE}}",
      "",
      "{{VISITOR_PROBLEM_ANSWER}}",
      "",
      "### {{TERMINAL_TITLE}}",
      "",
      "```text",
      "{{TERMINAL_TRANSCRIPT}}",
      "```"
    ]
  }
}
